server:
  port: 8080
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://localhost:${server.port}/eureka/
      # online use: defaultZone: http://eureka-server:${server.port}/eureka/
  server:
    renewalPercentThreshold: 0.49